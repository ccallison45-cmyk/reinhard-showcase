export function DemoBanner() {
  return (
    <div className="bg-accent text-primary text-center py-1.5 text-xs font-semibold tracking-wide">
      PREVIEW — Demo site with sample data. Photos and contact info are placeholders.
    </div>
  );
}
